<Window x:Class="DrawGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DrawGrid"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1200">
    <Window.DataContext>
        <Binding Path="Main" Source="{StaticResource Locator}" />
    </Window.DataContext>
    <Grid Background="Gray">
        <Canvas x:Name="MyCircle" />
        <Border SnapsToDevicePixels="True" BorderBrush="Black" BorderThickness="1" CornerRadius="5" Margin="5"
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="#66FFFFFF">
            <Canvas x:Name="MyGrid" Width="100" Height="200" />
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="16" ShadowDepth="0" Opacity="1" />
            </Border.Effect>
        </Border>
        <WrapPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
            <Button Command="{Binding CommandDrawPath}">左手轨迹</Button>
            <Button Command="{Binding CommandDrawCircle}">右手画圆</Button>
        </WrapPanel>
    </Grid>
</Window>